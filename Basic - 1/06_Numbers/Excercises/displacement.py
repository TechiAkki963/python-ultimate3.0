u = float(input('Enter teh initial velocity : '))
v = float(input('Enter the final velocity : '))
a = float(input('Enter the accelration : '))

displacement = ((v*v)-(u*u))/(2*a)

result =f"Displacement : {displacement}"
print(result)